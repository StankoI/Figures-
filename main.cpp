#include <iostream>
#include "include/FigureRandom.hpp"
#include <fstream>
#include <string>

int main()
{   

    // FigureRandom FR(6);
    // srand(time(NULL));
    // for(int i = 0; i < 12; i++)
    // {   
    //     std::unique_ptr<Figure> ptr = FR.create();
    //     std::cout << ptr->to_string() << '\n';
    // }
    
    return 0;
}